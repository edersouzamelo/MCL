# Capturas de tela

Execute:

```bash
pnpm screenshots
```

Nesta execução, as capturas não foram geradas porque o Chromium do Playwright não estava instalado e o download do navegador foi recusado pelo ambiente.
